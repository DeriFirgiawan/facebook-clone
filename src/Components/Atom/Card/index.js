export const Card = ({children}) => {
  return (
    <div className="card">
      {children}
    </div>
  )
}

export const CardBody = ({children}) => {
  return (
    <div className="card-body">
      {children}
    </div>
  )
}
